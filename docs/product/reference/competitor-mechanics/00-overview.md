# Механика редактора RoomToDo — обзор и индекс

> **Что это.** Реверс-инжиниринг поведенческой логики редактора-конкурента [roomtodo.com](https://roomtodo.com) по его **не минифицированному** исходнику (`plannercore.js` ~81k строк с оригинальными именами и комментариями; `user.js` — API/сохранение). Цель — иметь готовую к реализации спеку _механики_ нашего будущего редактора, а не описывать её с нуля.
>
> **Статус источника.** Это **референс чужой механики**, а не наши продуктовые решения. Из него мы выведем собственные feature-спеки в `docs/product/features/`. Код конкурента не копируем — здесь описано поведение/правила своими словами, с ссылками на реальные имена классов/функций и номера строк как доказательство, что это из живого кода.
>
> **Важно про доверие.** У каждой секции есть блок «Confidence & gaps»: что точно вычитано из кода, что додумано (inferred), чего не нашли. Перед тем как закладывать что-то в архитектуру — свериться с этим блоком.
>
> **Статус валидации (2026-08-13).** Все доки 00–15 и deep-dives сверены с исходником отдельными субагентами: ~900 утверждений (якоря строк, имена символов, числовые константы) перепроверены по `plannercore.js` / `user.js` / `tris.js`. Найденные ошибки исправлены прямо в текстах; Confidence-блокам можно доверять.

## Единицы и система координат

- Все внутренние расстояния — **сантиметры**. Экранные пороги — **пиксели**, переводятся в мир делением на текущий зум `WC.wallsEditor.scale`.
- Координаты округляются до **0.001 см** на каждом снапе (`TR.roundCoord`).
- Плоскость плана: `+X` → мир `+X`; план `+Y` (см) → мир `−Z`; мир `+Y` — высота/верх.

## Ключевые глобальные константы

| Константа                 | Значение    | Смысл                                                                              |
| ------------------------- | ----------- | ---------------------------------------------------------------------------------- |
| `WC.SNAP_DIST`            | 10 px       | базовый радиус снаппинга                                                           |
| `WC.POINT_SIZE`           | 6 px        | радиус хит-теста угла                                                              |
| `WC.MAX_WALL_WIDTH`       | 80 cm       | макс. толщина стены                                                                |
| `WC.MIN_WALL_LENGTH`      | 15 cm       | мин. длина стены                                                                   |
| `WC.DEFAULT_WALLS_HEIGHT` | 280 cm      | высота стен по умолчанию                                                           |
| `CLICK_SHIFT`             | ~3 px       | порог «клик vs драг» (2D-конструктор)                                              |
| product click-vs-drag     | ~5 px       | порог в режиме меблировки                                                          |
| `SNAP.Snap2D` дистанция   | 10 → 15 cm  | магнетизм в плане: дефолт конструктора 10 (9477), для мебели в рантайме 15 (41668) |
| `radiusDropElement`       | 10 cm       | радиус привязки проёма к стене                                                     |
| `TR.L_EPS` / `TR.B_EPS`   | 1e-8 / 1e-4 | эпсилоны геометрии                                                                 |

## Архитектура (сквозная для всех секций)

- **Движок отделён от UI.** Ядро (`R2D` / `WC` / `TR`) владеет сценой, геометрией, историей; React-часть — тонкий контроллер поверх фасада `R2D.createPlannerAPI(planner)` — **мутатор без `return`**, навешивает на `planner` **7 неймспейсов** (`scene, view3d, view2d, viewWalk, constr, units, renders`; `plannercore.js` L602-608) плюс не-неймспейсные методы верхнего уровня (`zoomIn/zoomOut/fullscreen/toCenter/…`, L610+). Из семи EventDispatcher-ами являются ровно два — `scene`/`apiScene` (L602) и `constr`/`apiConstr` (L606), остальные пять — простые `{}`.
- **Две-слойная модель конструкции:**
  - _Топология_ (`WC.WallsCore`): граф `CPoint(x,y,type)` (children/parent/contour/areas[] — поля, не аргументы конструктора; 56814–56825) / `CWall(point1,point2,data)` / `CRoom(outer,data)` — наследует `CContour`, а `points[]`/`triangles[]`/`holes[]` — поля контура (56946, 56890–56898).
  - _Атрибуты_ (`WC.WallsData`): `DataWall`/`DataCut`/`DataFrame`/`DataPlug`/`DataCover`/`DataCeiling`/`DataPlinth`/`DataArea` — материалы, высоты, видимость; у каждого свой `build3D()`. Флаг `changed` (52795 у `DataWall`, аналогично `DataCover`) — маркер «материал менялся» для undo/redo материалов в `SceneHistory` (ставится сеттерами материала 52829+, сбрасывается `setAllElementsUnchanged` 55059 из истории 12359/12399, читается как `wasChanged`/`willBeChanged` 12650/12724, пишется в снимок `makeState` 12923), а **не** гейт частичной пересборки геометрии: `setStructure` (55088) делает `removeAll → findAxes → createPlinths → build3D` с нуля, `rebuildWallsAndCovers` (59533) — полностью синхронная линейная цепочка.
- **Одна 3D-сцена, много камер:** 2D-план — это НЕ отдельная отрисовка, а та же 3D-сцена сверху через ортокамеру; отдельный Canvas-2D только под черчение стен.
- **⚠️ Контур-first, а не стена-first.** Пользователь рисует замкнутые контуры, а стены/комнаты/полы **регенерируются** из них на каждый чих (`rebuildWallsAndCovers` → триангуляция → трассировка контуров). Комнаты выводятся, а не хранятся. Это фундаментальная развилка (контур-first vs стена-first) — выбрать осознанно.
- **Render-on-demand:** нет постоянного цикла — правка запускает N кадров и стоп (десктоп/мобайл разное число). Один общий `THREE.WebGLRenderer` — для трёх главных вьюверов (3D/2D/обход); плюс ≥3 вспомогательных рендерера у превью: ModelUploader (19715, 19781), MaterialUploaderPreview3d (21426), превью в `R2D.Tool` (29435).

## Индекс секций

| #   | Файл                                 | Домен                                                                                                |
| --- | ------------------------------------ | ---------------------------------------------------------------------------------------------------- |
| 00  | `00-overview.md`                     | Этот обзор + индекс                                                                                  |
| 01  | `01-walls.md`                        | Создание и редактирование стен                                                                       |
| 02  | `02-doors-windows.md`                | Проёмы: двери и окна                                                                                 |
| 03  | `03-rooms-floors-structure.md`       | Комнаты / полы / потолки / зоны + детекция комнат + плинтусы/соединители/врезки                      |
| 04  | `04-furniture-placement.md`          | Расстановка и манипуляции с мебелью                                                                  |
| 05  | `05-selection-transform-grouping.md` | Выбор / гизмо трансформации / quick-panels / группы / копипейст                                      |
| 06  | `06-materials-textures.md`           | Материалы и текстуры                                                                                 |
| 07  | `07-measurements-rulers.md`          | Размеры / линейки / измерения                                                                        |
| 08  | `08-cameras-views.md`                | Камеры / виды (2D, 3D, обход, 360)                                                                   |
| 09  | `09-undo-redo-history.md`            | Undo / redo / история                                                                                |
| 10  | `10-serialization-save-format.md`    | Сериализация / формат сохранения (полная схема)                                                      |
| 11  | `11-floorplan-import-align.md`       | Импорт и калибровка плана-картинки                                                                   |
| 12  | `12-catalog-assets.md`               | Каталог и загрузка ассетов (пакеты, glTF, пулы, загрузки)                                            |
| 13  | `13-uploads-ai-render-export.md`     | Загрузки кастома / AI фото→3D / фотореал-рендер 2K/4K/360 / туры / экспорт (монетизируемые «выходы») |
| 14  | `14-accounts-monetization.md`        | Аккаунты, авторизация, планы+кредиты, избранное, автосейв-владение, white-label, воронки             |
| 15  | `15-ui-redux-bridges.md`             | React/Redux-UI, панели/попапы, и 6 границ postMessage (host / конфигуратор / оплата / туры / legacy) |

> **Расширение охвата (секции 13–15).** Секции 00–12 описывают **механику движка** (геометрия/сцена/взаимодействие). Секции 13–15 добавляют **продуктовый/UI/бизнес-слой** конкурента: монетизацию выходов, аккаунты, и архитектуру React-обвязки. Ключевой архитектурный факт из 15: **React-UI и движок `R2D` живут в ОДНОМ окне** и общаются через внутрипроцессную шину `apiScene`/`apiConstr` (не postMessage); postMessage — только на 6 внешних межоконных границах. Это уточняет тезис «движок отделён от UI, React — тонкий контроллер поверх фасада» ниже.

**Шаблон секций 00–12:** Interaction (state machine) / Snapping & constraints (с числовыми порогами) / Data model / Geometry rebuild (2D+3D) / Edge cases / Confidence & gaps. **Секции 13–15** используют адаптированный шаблон (Обзор / Технический дизайн / Модель данных / Продуктовые решения / Что не копируем / Confidence & gaps).

## Сводная карта уверенности и пробелов

> Читать перед тем, как закладывать что-то в архитектуру. «High» — вычитано из кода дословно; остальное — под проверку.

**Сквозные пробелы (повторяются в нескольких секциях):**

1. **Литералы URL эндпоинтов** (`R2D.URL.URL_SAVE_PLAN` и пр.) — в этих файлах их нет, они в отдельном бутстрап/конфиг-бандле. Подтверждены имена констант + метод/тело запроса, но не пути. Нужен конфиг-скрипт, если понадобятся точные URL. (секции 10, 12, 08)
2. **Глубокая 3D-математика вершин/UV** — `findTriWall`/`findTriCover`/`findPlinthSegment`, митринг в `blocksFromContour`, ядро Делоне `TR.triangulate`/`imported_*`, `imported_clean_graph` — **чёрные ящики**: известны контракты, не внутренности. Для пиксель-точного паритета их пришлось бы трассировать или вывести заново. (секции 01, 03, 06)
3. **Заявленный, но спящий код** — `maxActions=100` (лимит истории **не форсится**, надо добавлять trim самим), label `productSizes` (нигде не вызывается в этой сборке), тип-5 `PLINTH` (объявлен, парсера/писателя нет). Не считать это рабочим — это намерение, а не факт. (секции 09, 12)
4. **Порт на современный three.js** — их sRGB/тонмаппинг **легаси и непоследовательны** (энкодинг только на glTF-картах, в конструкторе рендерера не задан). При переносе на r185 цветовое пространство надо решать явно — см. [`../../architecture/threejs-r185-migration.md`](../../architecture/threejs-r185-migration.md). (секции 06, 08)

**Подтверждённые «этого у них нет»** (полезно — не тратить силы, если не нужно):

- Регулярной **сетки-снапа для мебели** нет — снап только к стенам/боксам предметов/углам (04).
- **Коллизий стен в режиме обхода** нет — свободный полёт на фикс-высоте, сквозь стены (08).
- **360°-захвата** нет — панорамы это каталожные ассеты на вывернутой сфере (08).
- **Авто-подписей углов** и внешних размерных цепочек нет (07).
- `private_material` как флага **материала** нет — владение через `isOwner()`/`user_key`; сама строка существует, но как фича тарифа в `user.js:50` (`add_mat = features.includes('private_material')`) (06, 14).
- **CSG нет** — дырки проёмов вычитаются триангуляцией грани вокруг них (01, 02).

**Домены с высокой уверенностью (можно опираться):** модель данных и формат сохранения (10), детекция комнат-пайплайн (03), снаппинг стен и state-машины инструментов (01), система выбора/групп/копипейст (05), бинарный формат ассетов и пулы (12), undo-архитектура (09), константы камер/видов (08). Числовые пороги во всех секциях вычитаны из кода.

**Чего не хватает для реализации** (сквозные гэпы валидации, не закрытые дайвами — добирать/решать при проектировании):

1. **Перечень событий `apiScene`/`apiConstr`** (имена, payload) — главный API-контракт движок↔UI; частично в 15, полного каталога нет.
2. ~~**Жизненный цикл `changed`-флагов** — кто ставит/сбрасывает, гранулярность ребилда.~~ **Закрыт (2026-08-17):** `changed` на `DataWall`/`DataCover` (52795) — флаг «материал менялся» для undo/redo материалов в `SceneHistory` (`setAllElementsUnchanged` 12359/12399, `wasChanged`/`willBeChanged` 12650/12724, `makeState` 12923); гранулярности ребилда у конкурента нет — `setStructure` (55088) пересобирает 3D с нуля (`removeAll → findAxes → createPlinths → build3D`), `rebuildWallsAndCovers` (59533) полностью синхронный, ни debounce, ни Worker (единственный Worker в файле — DRACOLoader, 22536).
3. **Маппинг WC-мира в THREE-сцену** — масштаб (см = юниты?), origin, слои `scene3d.bottom/middle` (36638–36641), clearDepth-сортировка.
4. **Экранные vs мировые пороги** — сводной таблицы «что делить на zoom» нет; собирать при портировании.
5. **Список легаси-API для r185** (encoding→colorSpace и пр.) с привязкой к местам кода — см. [`threejs-r185-migration`](../../architecture/threejs-r185-migration.md).
6. **Bootstrap** — создание пустого проекта, порядок инициализации синглтонов (60583, 66–76).

Уже закрыто дайвами второй волны: контракт `TR.rebuildContours` и выбор триангулятора → dd01/dd08; сквозной пайплайн правки/пересборки и оси → dd07; внутренности снапа → dd09; build3D покрытий → dd05; дырки/лутки проёмов → dd06; smart-move мебели → dd10.

## Deep-dives — вскрытые «чёрные ящики» (`deep-dives/`)

Низкоуровневые геометрические ядра, вскрытые отдельными глубокими проходами. Главный вопрос каждого — **стандарт или кастом**, чтобы понять, где брать готовую либу, а где переносить их 5-летний опыт.

| Файл                                  | Ядро                  | Вердикт      | Где живёт 5-летний опыт / что делаем                                                                                                                                                                                                                                              |
| ------------------------------------- | --------------------- | ------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `deep-dives/01-triangulation-core.md` | Триангуляция (CDT)    | **Стандарт** | Готовая `cdt2d` + `clean-pslg`. Опыт — в **обвязке**: `roundCoord` 0.001, дедуп рёбер, `resplitSegments` (T-стыки), классификация fill/hole по центроиду, отбраковка слайверов. Берём либу, обвязку воспроизводим; чиним O(n²)-дедуп.                                             |
| `deep-dives/02-wall-mitering.md`      | Митринг углов / офсет | **Кастом**   | Самописный per-segment офсет + пересечение линий, band **односторонний**. Несущие правила: ре-митр `0.75π` + тест на переброс спайка, near-parallel `0.05 рад`, `maxConnectorLength=40`. Готовая polygon-offset либа результат **не** повторит — три правила переносим осознанно. |
| `deep-dives/03-contour-tracing.md`    | «Что есть комната»    | **Кастом**   | Flood-fill групп треугольников через не-стеновые рёбра → обход границы по углам поворота (флип обмотки для дырок) → ре-привязка комнат по нечёткому 10×10 перекрытию площадей. Пайплайн переносим; рекурсию → явный стек.                                                         |
| `deep-dives/04-3d-mesh-uv.md`         | 2D→3D меши + UV       | **Кастом**   | Не `ExtrudeGeometry` — ручная сборка вершин/UV в `BufferGeometry` per-triangle. UV = функция (вдоль-стены/высота)/`pixPerMeter=100`, непрерывны за проёмами; части-материалы на ±0.1 см против z-fight. Переписываем сами; риск порта — `texture.encoding`→`colorSpace` (r185).   |

**Итог по ящикам первой волны: 1 стандарт, 3 кастома.** Настоящий «ров» конкурента — не сложная CS (Делоне — это библиотека), а накопленная **политика/обвязка вокруг неё**: квантование и чистка входа, правила митров, нечёткая ре-привязка комнат, непрерывность UV за проёмами. Теперь это зафиксировано с номерами строк — можно переносить точечно, а не переоткрывать на граблях.

**Вторая волна** — дайвы, закрывающие гэпы, найденные при валидации (везде кастомная обвязка конкурента, вопрос «стандарт или кастом» тут не стоит):

| Файл                                   | Что закрывает                                                                                                                          |
| -------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| `deep-dives/05-cover-build3d.md`       | Cover/Ceiling/Area `build3D`: путь от готовых 2D-контуров покрытий до 3D-мешей (UV-формулы — в dd04)                                   |
| `deep-dives/06-opening-holes.md`       | Проёмы изнутри: модель на оси → дырка в стене (`getHoles`, `compareAndRebuildContours`) → лутка (`createFrame3D`)                      |
| `deep-dives/07-wall-axes-pipeline.md`  | Пайплайн осей стен `WC.Axis`: как оси строятся (`findAxes`), когда пересоздаются, кто их потребляет и как проёмы переживают пересборку |
| `deep-dives/08-geometry-predicates.md` | Справочник предикатов 2D-ядра `TR` (~49470–50600): каждая функция с формулой, эпсилоном и краевыми случаями                            |
| `deep-dives/09-wall-snap-internals.md` | Внутренности `WC.SnapTool`: `snapX`/`snapY` до формул, полный `getSnapPoint`, направляющие-гайды, округление и дубликаты               |
| `deep-dives/10-furniture-stacking.md`  | «Умное» перемещение и стекинг мебели: `G.shiftVectors` / `getVectorsSides` построчно, посадка на поверхность                           |
