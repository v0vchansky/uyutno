# Редактор планировок · требования к реализации · P0

Роут `/project/:id` (закрыт авторизацией) и `/demo`. Макет — `Planner P0 Constructor.dc.html`. Эталон холста — `planner-canvas-reference.md`.

Общие правила — `../../guidelines.md`, шапки платформы — `../../layout.md`. Реализация на HeroUI v3 и токенах `theme-uyutno.css`; инлайновые стили прототипа в код не переносятся.

## Расхождения с общими правилами

Два, оба по решениям автора, оба названы здесь явно:

1. **Шапка редактора своя и плотная — 48px**, не `PublicHeader` (решение 10). Правило `layout.md` «высота шапки одинакова на всех экранах одной ширины» на редактор не распространяется — исключение уже внесено в `layout.md`. Причина: редактор единственный экран с дефицитом вертикали.
2. **Панель инструментов — плавающий блок поверх холста, а не колонка рейла.** Гайдлайн даёт рейл 60px, но решение 6 требует список «иконка + название», а подписи в 60px не встают. Компромисс: рейл 60px остаётся и держит переключатель видов и тумблеры снапа иконками (решения 8 и 9), а список инструментов лежит поверх холста слева. Холст ширину не теряет — панель оверлей, как и каталог (решение 13).

Плюс два отступления от шкал, объявленных здесь, чтобы они не выглядели случайными:

3. **Кнопки 32×32 в строке истории и в полоске контролов** при трёх высотах гайдлайна 36/40/44. Обе полосы сами имеют высоту 36px, кнопка внутри не может быть равна контейнеру. Это единственные места, где 32px допустимы; сенсорных целей в редакторе нет — он открывается только с 1024px.
4. **Радиус 8px у клавишных чипов и сегментов единиц** — из шкалы 8/12/24, взят как «мелкая плитка». Радиуса 6px в пакете больше нет.

Открытых вопросов к автору по P0 нет: 24 решения покрыли все развилки.

## Что на экране

Конструктор стен: холст, рейл видов и снапа, панель инструментов, поля ввода длины, подписи, полоска контролов холста, полоса подсказки клавиш.

**Чего нет:** сетки на холсте конструктора; стартовой модалки «как начать»; тостов; модалок подтверждения на удаление точки, разрез стены и отклонение контура; горячих клавиш на смену вида; кнопок отмены и повтора в шапке; переключателя видов в шапке; имперских единиц.

## Каркас

Фон страницы `--background`. Сверху шапка 48px, под ней строка: рейл 60px, затем холст. Холст — белая область с внутренней рамкой 1px `--border`, радиус 12px, внешний отступ 8px. Всё, что лежит на холсте, — оверлеи с абсолютным позиционированием.

| Место                        | Что                                                         |
| ---------------------------- | ----------------------------------------------------------- |
| слева сверху, отступ 12px    | панель инструментов, 224px                                  |
| справа снизу, отступ 12px    | колонка: место под сводку, под ней полоска контролов холста |
| по центру снизу, отступ 12px | полоса подсказки клавиш, максимум 560px                     |

Полоса подсказки центрируется по холсту и ограничена по ширине, поэтому с правой колонкой не пересекается даже на 1024px. Контейнер полосы — `pointer-events: none`, сама полоса — `auto`: клик мимо подсказки уходит в холст.

Тень всех плавающих панелей — `0 2px 8px rgb(0 0 0 / 0.10)` (вторая тень гайдлайна, заведённая под редактор). Радиус 12px.

Сырых HEX в DOM-части нет: подложка включённого тумблера — `--accent` 10%, выключенный тумблер — `--muted`, вторая строка тултипа — белый 72%. Чертёжные `--draft-*` живут только на холсте.

## Рейл

Ширина 60px, фон `--surface`, справа граница `--separator`, padding 8px по вертикали, кнопки 44×44 с шагом 2px (исключение шкалы отступов, заведённое под панель инструментов).

Сверху четыре кнопки видов: конструктор, 2D-план, 3D-обзор, walk. Активная — заливка `--accent`, иконка белая, `aria-pressed="true"`. Наведение на неактивную — заливка `--surface-secondary`, нажатие — `--surface-tertiary`; у активной наведение вида не меняет. Повторный клик по активной сбрасывает камеру этого вида. Разделитель 28×1px `--border` с отступом 8px.

Ниже четыре тумблера снапа: притяжение к углам, оси через точки, орто-лок, биссектриса. Включённый — иконка `--accent` на подложке #FBEDE6, выключенный — иконка #A8A8A8 без подложки. Дефолты: включены все, кроме орто-лока.

Заливку акцентом тумблеры не получают: она занята видами, и два разных смысла одним приёмом читались бы как один список.

## Панель инструментов

Ширина 224px, фон `--surface`, радиус 12px, padding 8px, шаг между строками 2px.

Сверху строка истории: отмена и повтор иконками 32×32, справа подпись «история» 11px `--muted`, снизу граница `--separator`. Обе кнопки `disabled`, когда откатывать нечего. Тултипы: «Отменить — Ctrl+Z», «Повторить — Ctrl+Y или Ctrl+Shift+Z» (⌘Z, ⇧⌘Z на macOS). Повтор слушает оба сочетания. Отмена и повтор живут только здесь, в шапке их нет.

Заголовок группы — 11px/600, uppercase, трекинг 0.06em, `--muted`, padding 4px 8px.

**Инструменты** — строки 36px, радиус 8px, иконка 20px и название 14px через 8px. Наведение — заливка `--surface-secondary`, нажатие — `--surface-tertiary`. Активный — заливка `--accent`, текст белый **14px/600**: контраст терракоты с белым 4.1:1, а гайдлайн разрешает на акценте текст от 15px либо полужирный от 14px. Три инструмента: «Стены», «Прямоугольная комната», «Комната по точкам». Тултип инструмента рисования упоминает Esc — выход из режима.

**Подписи** — три переключателя 32px: «Размеры», «Имя комнаты», «Площадь», все включены. Переключатель 32×18px, ползунок 14px белый. Включённый — дорожка `--accent`, выключенный — дорожка `--border`; ползунок в обоих положениях белый.

Панель обязана расти: «Зона», «Рисование пола», «Линейка A→B», «Импортировать план», «Откалибровать подложку», «Удалить подложку» встают новыми строками в существующие или новые группы. Если панель не влезает в высоту холста, она скроллится внутри себя; заголовки групп при скролле не липнут.

Панель клавиатуру не перехватывает: горячие клавиши слушает редактор глобально, а пока фокус в поле ввода — не слушает никто, кроме поля. Кнопки читаются как подсказка к клавише, а не как единственный способ действия.

## Иконки

Для чертёжных инструментов и тумблеров снапа в Lucide иконок нет, поэтому нарисованы **свои — в метрике Lucide**: сетка 24×24, **штрих 2**, скруглённые концы, `currentColor`, без заливок (кроме точек-маркеров в «Комнате по точкам»). Штрих именно 2, как у Lucide по умолчанию и как требует гайдлайн: иначе в одной панели встретились бы два веса — заимствованные иконки со штрихом 2 и свои с 1.5. Второго набора это не создаёт — гайдлайн разрешает свои SVG ровно для того, чего в наборе нет. Остальные иконки берутся из `lucide-react`: `ChevronLeft`, `Undo2`, `Redo2`, `Ruler`, `Maximize2`, `Minus`, `Plus`, `Search`, `Mouse` (для мышиных жестов в полосе подсказки).

## Тултипы

Иконка без подписи обязана иметь тултип — это весь рейл и кнопки истории. Формат: первая строка — название бытовым языком, вторая (`#D4D4D4` на тёмном фоне) — пояснение или клавиша. Плашка тёмная (`--foreground`), текст белый 12px, радиус 8px, padding 8px 12px, максимум 260px, тень плавающей панели, стрелка 5px в сторону кнопки. Задержка появления 400ms, скрытие мгновенное; на `focus-visible` тултип показывается так же, как на наведении.

Тексты (они же `aria-label` и `title`):

| Кнопка       | Название                         | Вторая строка                                                          |
| ------------ | -------------------------------- | ---------------------------------------------------------------------- |
| вид 1        | Конструктор стен                 | Чертёж стен: контуры, точки, размеры. Повторный клик — сбросить камеру |
| вид 2        | 2D-план                          | План сверху с текстурами и мебелью, без размеров и хендлов             |
| вид 3        | 3D-обзор                         | Объёмная модель, камера вокруг плана                                   |
| вид 4        | Прогулка                         | Вид от глаз, ходьба по квартире                                        |
| снап 1       | Липнуть к углам                  | Выключение сжимает радиус до 2 см, а не отключает притяжение           |
| снап 2       | Ровно по горизонтали и вертикали | Оси через предыдущую и первую точку контура                            |
| снап 3       | Только прямые углы               | Жёсткая фиксация: наискось поставить нельзя                            |
| снап 4       | По центру угла                   | Линия делит ближайший угол пополам                                     |
| инструмент 1 | Стены                            | Ломаная с толщиной. Esc — выйти из режима                              |
| инструмент 2 | Прямоугольная комната            | Два клика по углам, стены сразу с толщиной                             |
| инструмент 3 | Комната по точкам                | Полигон без толщины, минимум четыре вершины                            |
| история      | Отменить / Повторить             | Ctrl+Z / Ctrl+Y (⌘ на macOS)                                           |
| подписи      | Размеры / Имя комнаты / Площадь  | что именно скрывается на холсте                                        |

Тултип снапа обязан сказать, что выключение не отключает притяжение совсем: иначе тумблер обещает больше, чем делает.

Тултип и полоса подсказки клавиш не дублируются: тултип объясняет кнопку по наведению, полоса перечисляет клавиши текущего состояния и появляется сама.

## Поле ввода длины

Высота 26px, радиус 8px, фон `--surface`, шрифт 12px с табличными цифрами, суффикс единиц 11px `--muted` **снаружи рамки**. Поле повёрнуто под углом ребра, с автоматическим разворотом, чтобы текст не встал вверх ногами.

| Состояние           | Вид                                                |
| ------------------- | -------------------------------------------------- |
| подпись             | без фона и рамки, читается как текст               |
| наведение           | рамка 1px `--accent`                               |
| фокус / ввод        | кольцо 2px `--accent`, текст выделен, курсор ввода |
| пассивное поле пары | рамка 1px `--border`, текст `--muted`              |
| ошибка              | кольцо 2px danger плюс плашка с текстом рядом      |
| скрыто по порогу    | не рисуется, введённое значение сохраняется        |

**Пара полей на сегменте.** У полой комнаты у каждого сегмента два поля: ребро внешнего кольца контура и ребро внутреннего (решение 23). Оба редактируемые, каждое правит длину ребра своего контура одной и той же командой (решение 24). Фокус в каждый момент один: активное поле с кольцом и курсором, второе рядом пассивно. **Пассивное пересчитывается на лету**, пока печатают в активном, — не после Enter.

Не подписываем, какая величина «главная», и не рисуем переключатель «внешняя / внутренняя»: переключение делает Tab, и оно ничем не отличается от Tab между полями соседних рёбер. Ось стены пользователю не показывается и не редактируется.

У прямоугольной комнаты два инпута — ширина и высота; на полой геометрии их становится четыре, по паре на ось. Сцена 14 показывает, что четыре поля помещаются и не наезжают.

**Второй инпут** появляется, когда в контуре больше двух точек: им двигают первую точку и отращивают контур с обратной стороны.

**Ввод.** Enter ставит точку — как клик, включая правило замыкания контура. Escape отменяет. Tab переходит к следующему видимому полю по кругу, включая второе поле пары. **Потеря фокуса применяет**, а не отменяет. Стрелки внутри поля двигают курсор в тексте (решение 20), полями они не переключают. Пока фокус в поле, глобальные горячие клавиши не срабатывают вообще.

При правке готовой геометрии принимаются три формы: абсолютное число, `+N`, `-N`. У стены правка длины разводит оба конца симметрично, правка ширины сдвигает их перпендикулярно; у точки двигается только она.

**Валидация посимвольная** — исключение из правила платформы «валидация на отправке». Некорректный символ игнорируется, залипнуть на невалидной строке нельзя. Ошибку дают: пустая строка, одиночный `+` или `-`, отрицательное значение там, где не разрешено, результат короче 15 см (включая `0`), ввод, дающий самопересечение, значение больше 1 км.

**Плашка ошибки** (решение 5): кольцо danger на поле плюс плашка рядом — фон `--surface`, тень плавающей панели, текст 12px цветом danger. **Плашка не наследует поворот поля, она всегда горизонтальна.** Тексты говорят, что делать, а не только что не так: «Не короче 15 см», «Не больше километра», «Контур сам себя пересечёт — поставьте точку иначе». Геометрия не меняется, поле откатывается на последнее валидное значение.

**Подсказка-стрелка** во время печати показывает, в какую сторону сдвинется геометрия, и обновляется на каждый символ, а не после Enter.

При зуме и панорамировании открытые поля перепозиционируются, оставаясь поверх своих сторон.

## Подписи

**Подпись длины** — 12px, посередине сегмента, сдвинута перпендикулярно наружу, повёрнута по углу стены с автоматическим разворотом. Обновляется в реальном времени при перетаскивании. Состояния: видима; скрыта по порогу (сегмент короче ~30px или текст не влезает); прижата к краю (центр стены ушёл за границу — подпись подтягивается к пересечению стены с рамкой холста); скрыта тумблером «Размеры».

**Подпись комнаты** — имя 13px/500 и площадь 12px `--muted` в центре комнаты, два независимых тумблера видимости.

**Коллизия.** Спецификация правила не задаёт, предлагаю: **уступает подпись комнаты**. Размер нужен для работы, имя справочное. Порядок уступки — сначала гаснет имя, затем площадь; подписи сторон не сдвигаются и не уменьшаются никогда. Если налезают подписи двух сторон, скрывается та, чей сегмент короче.

**Формат.** Длины в текущих единицах: см (по умолчанию), м, мм. Площадь всегда в м² и не переключается. Единица через пробел: `210 × 92 см`, `20,3 м²`. Десятичный разделитель на показ — запятая, на вводе принимаются и точка, и запятая. Имперских единиц нет.

## Полоса подсказки клавиш

Полоса по нижнему краю холста, по центру: высота 32px, радиус 12px, фон `--surface`, тень плавающей панели, padding 0 12px, максимум 560px.

Внутри пары «действие → клавиша» через разделитель 1×12px `--border`: текст 13px и клавиша в рамке — минимум 22×20px, радиус 6px, рамка 1px `--border`, без заливки, моношрифт 11px. Мышиные жесты — иконкой мыши с подсвеченной кнопкой, а не словами.

**Максимум три пары**, всегда одна строка. При переполнении пары обрезаются по приоритету справа, перенос на вторую строку запрещён.

| Состояние                         | Подсказка                                                        |
| --------------------------------- | ---------------------------------------------------------------- |
| `editing`, ничего не выбрано      | «Панорамирование — ПКМ» либо пусто                               |
| `making-walls`, `making-room`     | «Отменить рисование — Esc», «Без снапа — Ctrl»                   |
| `making-rect`                     | то же плюс «Другое поле — Tab»                                   |
| фокус в поле длины                | «Другое поле — Tab», «Поставить точку — Enter», «Отменить — Esc» |
| `dragging-point`, `dragging-wall` | «Без снапа — Ctrl», «Отменить жест — Esc»                        |
| выделено ребро, правка длины      | «Применить — Enter», «Отменить — Esc», формы `+N` / `−N`         |

Состояние «подсказки нет» — полоса не рисуется совсем и места не занимает. Переход между состояниями мгновенный, без анимации.

Источников данных два, а не один: событие смены состояния автомата инструментов (`tools:changed`) и слой полей ввода длины — две строки таблицы автомат не различает. На внешний вид полосы это не влияет.

Не тултип, не тост, не модалка: не появляется по наведению, не исчезает по таймеру, не копится, не закрывается.

## Группа контролов холста

Одна полоска внизу справа (решение 7): высота 36px, радиус 12px, фон `--surface`, тень плавающей панели, padding 0 4px, кнопки 32×32 с шагом 2px, разделители 1×16px `--border`.

Порядок: линейка · зум «− % +» · «показать план целиком» · переключатель единиц `см / м / мм` · во весь экран. Подписи кнопок (они же `aria-label` и `title`):

| Кнопка        | Подпись               | Тултип                                  |
| ------------- | --------------------- | --------------------------------------- |
| линейка       | Линейка               | Измерить расстояние между двумя точками |
| зум «−»       | Отдалить              | Ctrl + колесо мыши                      |
| зум «+»       | Приблизить            | Ctrl + колесо мыши                      |
| «в центр»     | Показать план целиком | Вписать весь план в окно                |
| полноэкранный | Во весь экран         | F11                                     |

Сегменты единиц подписей не требуют — у них есть текст; `aria-pressed` у активного.

Состояния кнопок: наведение — заливка `--surface-secondary`, нажатие — `--surface-tertiary`. **На пределах шкалы зума** соответствующая кнопка получает `disabled`: на минимуме (5%) недоступна «−», на максимуме (6400%) — «+». Недоступная кнопка гасится до 55% прозрачности, курсор обычный.

Индикатор зума — минимум 44px, табличные цифры, подписывается как масштаб **плана**: 1,0× = 100%. Без индикатора 41 шаг шкалы нечитаем. Переключатель единиц — три сегмента 26px, активный на `--surface-tertiary`; переключение глобальное для проекта, все подписи и поля перерисовываются мгновенно.

Над полоской зарезервировано место под панель статистики (решение 11): 180×36px, пунктирная рамка `--border`, одно пустое состояние. Оно стоит в той же правой колонке, поэтому не конфликтует ни с полоской контролов, ни с центрированной полосой подсказки.

## Состояния экрана

**Пусто (новый проект).** Чистый холст без сетки плюс строка первого шага по центру: «Выберите инструмент и поставьте первую точку», 13px `--muted`. Исчезает после первой поставленной точки. Ни модалки, ни coach-marks.

**Идёт рисование.** Лента, живой сегмент, гайды снапа, поля ввода длины на рёбрах длиннее порога, подсказка клавиш в режиме рисования.

**Идёт жест.** Точка или сторона в состоянии перетаскивания, подписи пересчитываются на лету, подсказка клавиш в режиме драга.

**Ошибка геометрии.** Визуального состояния нет: отклонённый контур отбрасывается молча. Сцена 9 показывает, что пользователь видит вместо сообщения — прежнюю геометрию.

**После отмены.** Выделение снимается автоматически и не восстанавливается. Если серия отмен дошла до пустого холста, редактор сам выходит из режима рисования.

## Адаптив

Редактор открывается только от 1024px, комфортная ширина от 1280px, отдельной планшетной раскладки нет. Ниже 1024px — экран-заглушка (P1.8, следующий пакет). На 1024px после рейла 60px холсту остаётся 964px: панель инструментов 224px и полоска контролов около 380px оставляют центру примерно 340px свободной ширины, а полоса подсказки в 560px центрируется и в них укладывается.

## Доступность

Рейл — кнопки с `aria-pressed` у видов и `role="switch"` с `aria-checked` у тумблеров, у каждой `aria-label` полным текстом, потому что иконка без подписи. Панель инструментов — обычные кнопки, активный инструмент `aria-pressed`. Тумблеры подписей — `role="switch"`.

Поля ввода длины — обычные `input` с `aria-label` вида «длина внешнего ребра», ошибка связывается через `aria-describedby`, при ошибке `aria-invalid`. Tab обходит только видимые поля, по кругу.

Полоса подсказки — `aria-live="polite"`: смена состояния читается, но не перебивает.

Кольцо фокуса всюду акцентное. Контраст терракоты с белым 4.1:1, поэтому текст на акцентной заливке идёт либо от 15px, либо полужирным от 14px: название активного инструмента — 14px/600.

**Холст — графика с текстовым описанием** (решение 22): `role="img"`, `aria-label` «план: 3 комнаты, 45 м²». Полной клавиатурной навигации по хендлам в этой версии нет — обход по Tab конфликтовал бы с Tab между полями ввода длины. Это решение, а не упущение.

---

# Оболочка редактора (P1)

Дополнение ко всему, что выше, — принятая часть не переписывается. Макет — `Planner Editor Shell.dc.html`, группы s0–s7. Заказ — `../briefs/planner-editor-shell.md`, блок B.

**Что меняется в принятом:** ничего. Каркас, панель инструментов, группа контролов холста, полоса подсказки клавиш и место под сводку остаются как описаны выше. Оболочка добавляет к ним шапку в двух состояниях, индикатор сохранения, кнопку каталога в рейле, оверлей каталога и состояния экрана.

## Шапка

Высота остаётся заявленной выше — **ровно 48px по внешнему габариту**: `header` с `height: 48px` и `box-sizing: border-box`, внутри контейнер на всю высоту с боковым padding 12px и gap 16px. Внутренний контейнер нужен ради порога 1024px, где меняется только gap; в P0 шапка теперь такая же — обе части пакета используют одну разметку. Слева направо: возврат «Проекты» со стрелкой 18px, разделитель 1×20px, имя проекта, распор, справа группа с gap 12px — статус сохранения, кнопка «Сохранить», аватар 32px.

Чего в шапке нет: переключателя видов (он в рейле, решение 8), отмены и повтора (только в панели инструментов), «Поделиться», «Скриншот», «Настройки проекта».

**Два состояния — один компонент.** Демо получается снятием и подменой элементов:

| Что                 | `/project/:id`                   | `/demo`                             |
| ------------------- | -------------------------------- | ----------------------------------- |
| правый край         | статус, «Сохранить», аватар      | «Сохранить», «Войти», «Регистрация» |
| признак режима      | —                                | плашка «Демо» справа от имени       |
| кнопка сохранения   | сохраняет                        | открывает модалку входа             |
| индикатор автосейва | есть                             | нет                                 |
| имя                 | кнопка, открывает переименование | текст, не редактируется             |

**Плашка «Демо»** — высота 22px, радиус 8px, фон `--accent` 10%, текст 11px/600 uppercase с трекингом 0.04em цветом `--accent`. Стоит справа от имени и не переносится.

**На пороге 1024px** первым схлопывается слово «Проекты» — остаётся стрелка возврата (`aria-label` сохраняется), gap группы уменьшается с 16 до 8px, максимальная ширина имени с 280 до 150px. Кнопка, статус и аватар не сжимаются: статус — единственный носитель ошибки сохранения, а кнопка — главное действие. Ниже 1024px редактор не открывается вовсе.

## Имя проекта

Кнопка высотой 32px, радиус 8px, текст 14px/500, `max-width: 280px` с обрезкой многоточием в одну строку. Наведение — подложка `--surface-secondary`; фокус с клавиатуры — кольцо 2px `--accent`; клик открывает **существующую модалку переименования** из списка проектов, новой не рисуем — компонент уже есть в коде: `apps/platform/src/client/projects/components/RenameProjectModal/RenameProjectModal.tsx`.

Сверено с реализацией: ширина 344px, радиус 24px, padding 24px, gap 16px, поле HeroUI `TextField` с лейблом «Название», кнопки 40px в порядке «Отмена» (`--surface-secondary`) → «Сохранить», фокус и выделение всего текста ставятся на монтировании формы. Расхождений с макетом нет; в прототипе поправлены только тексты ошибок — берутся из кода: «Введите название» и «Максимум 60 символов» (предел `PROJECT_NAME_MAX_LENGTH` = 60), цвет подписи ошибки `oklch(38% 0.16 27)`. Ниже 1024px модалка становится нижним листом, кнопки растягиваются в ряд — это уже реализовано в компоненте, отдельного кадра не рисуем.

**Инлайн-редактирования в шапке нет** — такого паттерна на платформе не существует нигде.

В демо имя не кнопка, а текст: без подложки на наведении, без фокуса, без курсора-указателя. Оно не должно читаться как сломанная кнопка, поэтому у него нет ни рамки, ни hover-состояния — обычная надпись рядом с плашкой.

## Индикатор состояния сохранения

Живёт слева от кнопки «Сохранить», в одной строке. **Один статус на все запросы**: если ручное сохранение и автосейв в очереди, показывается общий «Сохраняем…», а не два индикатора.

| Состояние       | Что видно                                                                                                                                                      |
| --------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| покой           | статуса нет, кнопка активна                                                                                                                                    |
| идёт запись     | кнопка недоступна, внутри спиннер 14px и текст «Сохраняем…»                                                                                                    |
| сохранено       | иконка галочки `--accent` 16px и «Сохранено, 14:32», 12px `--muted`                                                                                            |
| автосохранено   | та же иконка цветом `--muted` и «Автосохранено, 14:32»                                                                                                         |
| ошибка фонового | иконка предупреждения 16px цветом danger, тултип «Не удалось сохранить на сервер, 14:32 — изменения сохранены локально»; снимается первым успешным сохранением |
| офлайн          | иконка «нет сети» и «Нет сети, изменения не сохранены» цветом danger — держится, пока сети нет, снимается сама                                                 |
| демо            | статуса нет вовсе                                                                                                                                              |

Ручное и автоматическое сохранение различаются словом и цветом иконки: галочка акцентная у ручного, приглушённая у авто. Двух разных иконок не вводим — событие одно и то же, разная у него только причина.

**Принцип:** ручное действие пользователя → модалка; фоновое → тихая иконка или молчание. Тостов в редакторе нет, поэтому носителей ровно два.

**Офлайн — единственное место, где статус и модалка показываются одновременно.** Статус держит признак состояния, модалка отвечает на действие: при ручном сохранении (кнопка или Ctrl+S) поверх статуса поднимается «Нет сети» — изменения не сохранены, вкладку закрывать нельзя, сохранение продолжится само. **Обещания «изменения сохранены локально» ни в статусе, ни в модалке нет**: локальный черновик есть только на демо-роуте, в обычном проекте обещать нечего. В тултипе фоновой ошибки это обещание остаётся — там черновик действительно есть.

Ошибка ручного сохранения — модалка «Не удалось сохранить» с кнопками «Отмена» и «Повторить». Частный случай — «Проект удалён» с единственной кнопкой «В галерею».

**Отложенное сохранение** (Ctrl+S нажат посреди перетаскивания) отдельного состояния не получает: пока идёт жест, статус не меняется, и только после отпускания мыши включается «Сохраняем…». Промежуточного «ждём отпускания» не рисуем — оно бы мигало на каждом перетаскивании.

## Модалка входа из демо

Открывается кнопкой сохранения и Ctrl+S на `/demo`. Кнопка активная и кликабельная, не `disabled`: иначе Ctrl+S остаётся без ответа и причина не объясняется.

Ширина 360px, общий паттерн модалок платформы — затемнение 40%, карточка по центру, радиус 24px, padding 24px, gap 16px. Внутри ровно три вещи: заголовок «Войдите, чтобы сохранить проект», строка-успокоение «Работа не потеряется: она останется здесь, пока вы входите, и вернётся на место», одна кнопка «Войти» 40px, ведущая на `/login`.

Полей, кнопок провайдеров и переключателя «вход / регистрация» внутри нет — всё это уже на `/login`. Поэтому состояний «идёт вход» и «ошибка входа» у модалки не бывает.

| Состояние               | Что видно                                                                                                                  |
| ----------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| обычное                 | заголовок, строка-успокоение, «Войти»                                                                                      |
| «Сохраняем ваш проект…» | заголовок меняется, вместо кнопки — спиннер 16px и «Переносим в аккаунт»; держится до перехода на `/project/:newId`        |
| хранилище недоступно    | 400px: тот же заголовок плюс честный текст, что работа не переживёт перехода; две кнопки — «Продолжить рисовать» и «Войти» |

В состоянии «хранилище недоступно» слова «не сохранится» выделены полужирным: это единственное, что пользователь обязан прочитать. Кнопка «Войти» остаётся — решение уходить всё равно за ним.

Закрытие без входа возвращает к рисованию и отдельным состоянием не считается. Диалога «оставить черновик? / отбросить?» не существует — перенос молчаливый.

## Каталог

Оверлей поверх холста справа: ширина 320px, отступы 12px от краёв холста, высота во всю его высоту, фон `--surface`, радиус 12px, тень плавающей панели. **Холст не отодвигается и не сжимается** — оверлей просто лежит поверх, как требует решение 13. Правило «панели не сворачиваются» не трогаем: состояния «свёрнута» нет, есть «закрыт» и «открыт».

Открывающий элемент — **кнопка в рейле**, ниже переключателя видов, отделена разделителем. Состояния как у остальных кнопок рейла плюс активное (оверлей открыт) — заливка `--accent`, `aria-pressed="true"`.

Внутри: заголовок «Каталог» 15px/600 и кнопка закрытия 32px в шапке оверлея с границей снизу; поле поиска 36px с иконкой слева; список плитками в две колонки, gap 12px. Плитки **безликие** — наполнение каталога не заказывалось: серый прямоугольник 4:3 и полоска подписи.

| Состояние         | Что видно                                                                                                            |
| ----------------- | -------------------------------------------------------------------------------------------------------------------- |
| закрыт            | оверлея нет, кнопка рейла в обычном состоянии, правая колонка с контролами холста и местом под сводку на своём месте |
| открыт со списком | восемь плиток-заглушек, поиск пустой                                                                                 |
| загрузка          | шесть скелетонов с той же пульсацией, что в проектах                                                                 |
| пусто             | «Каталог пуст» и строка, что объекты появятся позже                                                                  |
| пусто по запросу  | «Ничего не нашлось» и предложение изменить слово, запрос остаётся в поле                                             |

**Что с холстом под оверлеем.** Он остаётся видимым и не затемняется: каталог — рабочий инструмент, а не модалка, и пользователь должен видеть план, чтобы выбирать мебель. Правая колонка с контролами холста и местом под сводку уезжает под оверлей и возвращается при закрытии; полоса подсказки клавиш центрируется по холсту и оверлеем не перекрывается.

**Фокус и клавиатура.** Оверлей — не модальный диалог: `role="dialog"` с `aria-modal="false"`, ловушки фокуса нет, работать с холстом при открытом каталоге можно. При открытии фокус переходит в поле поиска, при закрытии возвращается на кнопку рейла. Esc закрывает оверлей; если фокус в поле поиска и там есть текст, первый Esc очищает поле, второй закрывает. Повторное нажатие кнопки рейла тоже закрывает.

## Состояния экрана · оболочка

**Загрузка проекта** — **чистый белый экран под шапкой**, без рамки холста и внутреннего отступа; по центру индикатор: спиннер 28px, название фазы 14px/500, счётчик «Шаг N из 2» 12px `--muted` и полоска прогресса 180×4px. Рейл, панель инструментов и контролы холста в этот момент не рисуются: их содержимое зависит от загруженной сцены, а активный вид и положение тумблеров восстанавливаются вместе с ней. На экране остаётся только шапка. Две явные фазы: «Загружаем проект» → «Подгружаем модели и материалы». Редактор становится интерактивным только когда сцена восстановлена полностью; полузагруженного состояния не бывает.

**Чем заканчивается загрузка.** Новый проект открывается в конструкторе. Сохранённый — в том виде и с той камерой, на которых его оставили, поэтому активной в рейле может оказаться кнопка не конструктора. Раскрытые панели и выделение не восстанавливаются: оверлей каталога после загрузки всегда закрыт.

**Загрузка после входа из демо** отдельного экрана не получает: модалка держит «Сохраняем ваш проект…» до перехода, дальше отрабатывает тот же индикатор с двумя фазами. Стык читается как одно движение — спиннер не пропадает между ними.

**Ошибка открытия** — модалка «Не удалось открыть проект», кнопки «В галерею» и «Повторить».

**Частичная ошибка** — плашка поверх холста: иконка предупреждения danger, «3 объекта не удалось загрузить», кнопка «Повторить». Плейсхолдеры стоят на местах объектов. Объект, удалённый из каталога навсегда, плейсхолдера не получает и просто не создаётся.

**Устаревшая версия формата** — модалка «Обновите редактор» с кнопкой «Обновить страницу».

**Заглушка ниже 1024px** — отдельный экран: логотип в шапке платформы, иллюстрация-заглушка, заголовок «Редактор работает на компьютере», абзац про то, что чертить пальцем неудобно, и одна кнопка. Тон по гайдлайну — факт, не извинение. **Ссылка по контексту:** вошедшему — «Мои проекты» на `/projects`, гостю из демо — «На главную» на `/`. Это одна вёрстка с подменой ссылки, а не два экрана.

**Пустой проект** описан выше в «Состояниях экрана» P0 и не переделывается: чистый холст без сетки плюс строка первого шага. С шапкой и загрузкой он не спорит — строка живёт в центре холста, шапка и рейл отрисованы сразу, индикатор загрузки к этому моменту снят.

## Место под сводку переживает оболочку

Подтверждение строкой, как требует заказ. Зарезервированный угол стоит в правой колонке вместе с контролами холста и остаётся на месте во всех состояниях оболочки: при открытом каталоге колонка уезжает под оверлей и возвращается при закрытии — сводка не переезжает и не всплывает поверх каталога; во время загрузки колонка отрисована, но накрыта индикатором вместе со всем холстом; в демо-режиме место не меняется, потому что демо снимает элементы только в шапке. Наполнение не заказывается.

## Доступность · оболочка

Шапка: возврат — ссылка с текстом (на пороге 1024px текст уходит, `aria-label="Проекты"` остаётся). Имя проекта — кнопка с `aria-label="Переименовать проект"`; в демо это не кнопка, а текст, поэтому в таб-обход не попадает.

Статус сохранения — `aria-live="polite"`, чтобы смена читалась и не перебивала. Иконка фоновой ошибки — `role="img"` с `aria-label`, а не только `title`: тултип по наведению недоступен с клавиатуры.

Модалки — `role="dialog"`, `aria-modal="true"`, ловушка фокуса, возврат фокуса на инициатора. У модалки входа из демо фокус при открытии ставится на «Войти» — единственное действие. У модалок ошибок — на подтверждающую кнопку.

Оверлей каталога — `role="dialog"` с `aria-modal="false"` (не модальный, холст доступен), `aria-label="Каталог"`, фокус в поле поиска при открытии и возврат на кнопку рейла при закрытии. Кнопка каталога — `aria-pressed`.

Заглушка ниже порога — обычная страница с `h1`, кнопка-ссылка 44px.

## Согласование P1 с P0

Общие элементы в обеих частях пакета совпадают байт в байт — сверено сравнением разметки: шапка и её внутренний контейнер, ссылка возврата, кнопка имени, рейл и кнопки 44×44, панель инструментов, активная строка инструмента, рамка холста, полоска контролов, полоса подсказки клавиш.

При сборке оболочки в прототип попала упрощённая панель инструментов: строки были неинтерактивными `div` без подписей, отсутствовали третий инструмент и вся группа «Подписи». Заменена принятой панелью из P0 во всех шести кадрах, где она встречается. Полосе подсказки возвращён предел 560px, ссылке возврата — подложка на наведении.

При правках любой из частей общие элементы правятся в обеих: расходиться им нельзя, это один и тот же компонент.

## Что перекрывает затемнение

Правило одно на все модалки редактора: **затемнение накрывает экран целиком** — шапку, рейл и холст. Модалка принадлежит экрану, а не холсту; если затемнить только холст, шапка и рейл выглядят доступными, хотя ловушка фокуса их уже отключила. Плюс так же ведут себя модалки переименования и удаления в списке проектов — второго поведения для одного паттерна не заводим.

Загрузка проекта — исключение, и осознанное. Затемнения там нет вовсе: **под индикатором пустой холст**, потому что показывать ещё нечего — панель инструментов, план и контролы появляются вместе с готовой сценой. Так честнее и проще, чем гасить макет проекта, которого пока нет. На экране только шапка, рейла нет; в шапке **рабочей остаётся ссылка «Проекты»**; остальные элементы шапки — имя, кнопка сохранения, аватар — гасятся до 40% и получают `pointer-events: none`. Причина: подгрузка моделей может подвиснуть, и единственным выходом из зависшего экрана не должна оказаться кнопка «назад» в браузере. Уйти в этот момент безопасно — правок ещё нет, терять нечего.

Оверлей каталога под это правило не попадает: он не модальный (`aria-modal="false"`), затемнения не даёт вовсе, холст под ним остаётся видимым и рабочим.
