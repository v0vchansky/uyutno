# Handoff · редактор планировок (планер)

Пакет по брифу `../briefs/planner-editor.md`. Приоритет P0 — конструктор стен.

## Состав

| Файл                             | Что внутри                                                                                                                                                                        |
| -------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Planner P0 Constructor.dc.html` | DOM-прототипы P0: каркас на 1440, рейл, панель инструментов, поля ввода длины, полоса подсказки клавиш, подписи, контролы холста; плюс 15 референсных сцен холста по чеклисту 8.1 |
| `planner-editor-ui.md`           | требования к реализации DOM-части                                                                                                                                                 |
| `planner-canvas-reference.md`    | эталон холста: чертёжные токены, таблица «состояние → атрибуты», курсоры, поведение при зуме                                                                                      |
| `Planner Editor Shell.dc.html`   | оболочка редактора P1: якорный экран 1440 и галерея состояний s1–s7 — шапка, имя проекта, индикатор сохранения, модалка входа из демо, каталог, состояния экрана                  |
| `support.js`                     | рантайм прототипов                                                                                                                                                                |

Прототипы открываются в браузере напрямую.

## Уровень проработки

**Оболочка (P1) — второй заказ**, приехала дополнением: прототип `Planner Editor Shell.dc.html` плюс раздел «Оболочка редактора (P1)» в `planner-editor-ui.md`. Принятые файлы не переписывались, изменений к принятому нет — только новые разделы.

**High-fidelity** по P0: геометрия, цвета и состояния финальные. P1 (шапка, сохранение, загрузка, каталог, заглушка) и P2 (свойства, плавающая панель, линейка, подложка, walk) в этот пакет не входят — идут следующими файлами.

## Что читать до реализации

Общие правила — `../../guidelines.md`, шапки и подвалы платформы — `../../layout.md`, токены — `apps/platform/src/client/theme-uyutno.css`. Прототипы не production-код: реализация на HeroUI v3, инлайновые стили не переносятся.

## Расхождения с общими правилами

Два, оба по решениям автора и оба описаны в `planner-editor-ui.md`: своя плотная шапка редактора 48px (решение 10) и панель инструментов оверлеем поверх холста вместо колонки рейла (следствие решений 6, 8, 9).

## Ответ на аудит

По `README-audit.md` от 2026-08-20 (статус «принято», 12 пунктов остатка) закрыто всё, кроме того, что аудит сам отнёс к P2. Правки текстовые и по состояниям, сцены не переделывались.

| # аудита | Что сделано                                                                                                                               |
| -------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| 1        | Тумблеры снапа: подписи и тултипы приведены к формулировкам брифа, «оси через точки» и «орто-лок» разведены явной таблицей                |
| 2        | Пять кнопок группы контролов холста получили подписи и тултипы (линейка, отдалить, приблизить, показать план целиком, во весь экран)      |
| 3        | Штрих собственных иконок 1.5 → **2**, как в Lucide и гайдлайне: два веса в одной панели исключены                                         |
| 4        | Добавлены состояния «наведение» и «нажатие» для рейла, строк инструментов и кнопок контролов; «зум на пределе шкалы» — кадры и `disabled` |
| 5        | Выключенное положение переключателя подписей: дорожка `--border`, кадр рядом с включённым                                                 |
| 6        | Активный инструмент — 14px/**600**; противоречие в разделе «Доступность» устранено                                                        |
| 7        | Сырых HEX в DOM-части нет: `--accent` 10%, `--muted`, белый 72%                                                                           |
| 8        | Отступления от шкал объявлены строками 3 и 4 в «Расхождениях»: кнопки 32×32 и радиус 8px вместо 6px (6px убран совсем)                    |
| 9        | `Mouse` добавлена в список иконок из `lucide-react`; повтор слушает `Ctrl+Shift+Z`; тексты ошибок переписаны на «что делать»              |
| 10       | Масштабируемые величины помечены «при 1,0× = 100%»                                                                                        |
| 11       | Подложка под подписью длины: есть всегда, `--draft-canvas` с отступом 4px; оговорено, к чему относилось «без фона и рамки»                |
| 12       | Слой растровой подложки — **под стенами**, с обоснованием (относится к P2)                                                                |

Дефекты прототипа из аудита тоже закрыты: перевёрнутые подписи отмены и повтора в трёх панелях исправлены, `disabled` появился, подписи кнопок больше не дублируются.

## Новое в теме

Эталон холста предлагает восемь чертёжных токенов (`--draft-*`) — их нужно добавить в `theme-uyutno.css`. Значения и обоснование в `planner-canvas-reference.md`.
