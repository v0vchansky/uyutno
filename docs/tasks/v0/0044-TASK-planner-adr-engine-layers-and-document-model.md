# 0044 · TASK · ADR A + B: слои движка планера и модель документа

- Статус: [x]
- Эпик: 0043
- Зависит от: 0045
- Спека: docs/product/features/planner/README.md; docs/product/architecture/planner-build-order.md (шаг 1); docs/product/architecture/competitor-practices-audit.md (вердикты по практикам roomtodo + «Решения автора»)
- Нужен дизайн: нет (документация)
- Дизайн: —
- PR: —

## Описание

Написать два ADR по шагу 1 планера и открыть их на обсуждение. Кода в этой задаче нет — только `docs/adr/0015-*.md`, `docs/adr/0016-*.md`, обновление индекса ADR и парковка отложенных вопросов. Это самые дорогие для отката решения планера: от них зависит всё остальное (undo, формат сохранения, проекция в Three, каталог, материалы).

Это бриф сессии: читать целиком перед стартом, работать по нему, не расширять скоуп.

### Что решаем

**ADR 0015 — Архитектура движка планера: слои, фасад, мост с React** (кандидат A):

1. **Слои и направление импортов**: документ + чистая геометрия (plain data, без Three/DOM) → движок-фасад (команды, будущие инструменты, история, шина событий) → проекция в Three (сцена, рендер, камеры) → React-UI (панели, инспектор). Кто кого может импортировать; энфорс через `no-restricted-imports` per-directory, как в ADR 0007. Именование главного объекта — по конвенции `Manager`, не `Service` (см. `apps/platform/CLAUDE.md`).
2. **Ядро + сменный UI-скин.** Планер — ядро; React-UI — один из возможных скинов. Критерий: UI не владеет состоянием документа и общается с ядром **только** через публичный API фасада + события. Публичный API нарезать по зонам ответственности неймспейсами (например `document`, `view`, `tools`, `history`, `selection` — состав определить; конкурент нарезал `scene/view3d/view2d/viewWalk/constr/units/renders`, см. competitor-mechanics/00 «Архитектура»). Правило: React никогда не мутирует документ напрямую — только через команды фасада.
3. **Отдельный пакет — сейчас или потом.** ADR 0007 говорит «в v1 переезжает в пакет». Решить здесь: заводить `packages/planner-core` (ядро без React) физически уже сейчас или держать в `apps/platform/src/client/planner/` со структурой «как пакет» и границей на ESLint. Учесть: сборка webpack + Jest в монорепе (ADR 0002), стоимость переезда позже, «UI как скин» проще доказывается отдельным пакетом.
4. **Мост движок ↔ React**: как React читает состояние движка и подписывается на изменения. Выбрать один механизм (`useSyncExternalStore` над фасадом / zustand / только шина событий) и объяснить, почему не Redux-обвязка конкурента (competitor-mechanics/15).
5. **Шина событий**: типизированная, минимальный набор событий для шага 1 (`document:changed`, `view:changed`, …) с правилом расширения; готовая либа или `EventTarget` — обосновать.
6. **Стек зависимостей планера для шага 1** — одной таблицей: Three r185 (дано), генератор id, immutable-обновления, шина, состояние для React — что берём и почему готовое, а не своё. Геометрические либы (`cdt2d`, `clean-pslg` и т.п.) здесь **не** решаются — это ADR C, когда они реально нужны.
7. **Инициализация и жизненный цикл**: как платформа создаёт планер (фабрика/класс `PlannerManager` + React-обёртка), порядок бутстрапа (документ → движок → проекция → UI), mount/dispose (renderer, геометрии, материалы, ResizeObserver, подписки). **Render-on-demand**: `invalidate()` → конечное число кадров и сон; ровно один `WebGLRenderer` на планер. Как это проверяется perf/leak-гвардом из testing-strategy.
8. **DI-контракт с платформой**: планер не импортирует `auth`/`common`/`project` (ADR 0007). Что он получает снаружи — минимум для шага 1 (например, `logger`, идентификатор проекта, колбэки загрузки/сохранения документа как интерфейс на будущее). Не проектировать больше, чем нужно шагу 1 и очевидно шагу 3.
9. **Тестируемость как ограничение**: слои документ/геометрия/фасад тестируются в Jest без Three (testing-strategy, оговорка про ESM Three); всё, что трогает Three, — только слой проекции. Правило покрытия — «вся математика и все ветки, не только happy path» (см. testing-strategy, «Что считается покрытым»).
10. **Раскладка папок** внутри модуля/пакета — коротко, как в ADR 0007 «Layout».

**ADR 0016 — Модель документа планера** (кандидат B):

1. **Единицы и координаты**: см — единственная внутренняя единица; квантование координат до 0.001 при снапе; плоскость плана `(x, y)` → мир Three `(x, height, −y)`, `+Y` мира — вверх; углы — градусы в UI, радианы внутри (или наоборот — решить).
2. **Схема идентификаторов**: стабильные строковые id у всех хранимых сущностей (готовая либа, не свой генератор); ссылки только по id, никогда по индексу массива.
3. **Contour-first дерево документа**: `Document { version, settings, floors: Floor[] }`, что живёт во `Floor` (контуры с точками; заготовки-«места» под проёмы, мебель, материалы — только позиция в дереве и принцип привязки, без детальной схемы). Явный список: **что хранится** vs **что выводится** (стены как физические тела, комнаты, грани, оси — выводятся и не сериализуются).
4. **Производные сущности без персистентной идентичности** (решения аудита Q3/Q35, 2026-08-17): стены-тела, грани, оси, комнаты — производные, пересоздаются на каждом rebuild и **не имеют стабильных id между сохранениями**. Проём в документе хранится мировыми координатами (+ elevation, scale, flip, каталожный id, материалы рамы) и при загрузке/пересборке пере-дропается на ближайшую ось (гейт 10 см); данные комнат/полов пере-привязываются по перекрытию площади; материалы стен — по совпадению концов грани. В ADR B зафиксировать это правило и место таких данных в дереве; runtime-привязку (axisPos) — как не-сериализуемое состояние движка.
5. **Snapshot-friendly структура**: документ — plain JSON-сериализуемые данные, immutable-обновления со структурным разделением (готовая либа типа immer или ручная дисциплина — выбрать), чтобы undo (ADR D, шаг 2) мог хранить снапшот как ссылку. Требование к производному состоянию: не в документе, memoized по ссылке.
6. **Поле `version`** есть с первого дня; политика миграций — в ADR F (шаг 3), здесь только факт наличия и правило «миграция — не история».
7. **Multi-floor**: `floors[]` в модели, в UI один этаж (зафиксировано в спеке) — как это отражено (активный этаж — состояние UI/движка, не документа).

### Что уже решено и не пересматривается

- Голый Three.js r185, без react-three-fiber — ADR 0003; карта отличий r185 — `docs/product/architecture/threejs-r185-migration.md`.
- Планер — изолированный модуль без импортов из бизнес-модулей, всё внешнее через DI-пропсы, в v1 переезжает в пакет — ADR 0007.
- Jest + core без Three, слои тестов и DoD — `docs/product/architecture/testing-strategy.md`.
- Шесть принципов и единицы из `features/planner/README.md` (контур-first, одна модель, один автомат, snap-first, реалтайм-rebuild, три вида — один документ; 1 см = 1 юнит, константы `SNAP_DIST=10`, `MIN_WALL_LENGTH=15`, `DEFAULT_WALL_WIDTH=10`, `MAX_WALL_WIDTH=80`, `DEFAULT_WALL_HEIGHT=280`).
- Undo: in-memory снапшоты, coalescing по mouseUp/blur, два контейнера, глубина 100 — `features/planner/09-undo-redo.md`. Здесь это только ограничение на модель (п. B5), сам undo — ADR D.
- Save: last-write-wins, миграция формата — silent — `features/planner/10-save-load.md`. Здесь только поле `version` (п. B6).
- Custom uploads, multi-floor UI, корзина — вне v0 (спека/бэклог).

### Что НЕ решаем в этой задаче (парковать, не решать)

Геометрический пайплайн и выбор триангулятора (C), детали undo (D), FSM инструментов (E), хранение сцены в БД (F), проекция doc→Three и UV (G), ассеты/каталог (H), выделение/пикинг (I), материалы (J), blob-хранилище (K). Если по ходу всплывает вопрос из этого списка — одна строка в раздел «Отложенные технические вопросы» файла `docs/product/architecture/planner-build-order.md` в формате `- (шаг N, из ADR 0015/0016) вопрос`, и идём дальше. Спеку `features/planner/*` в этой задаче не правим; если найдено противоречие спеки — тоже в парковку с пометкой «противоречие спеки».

### Что прочитать перед началом (обязательно)

Правила и формат:

- `docs/adr/README.md`, `docs/adr/template.md` — формат «Решение / Почему / Что важно знать», один экран, без секций «Альтернативы»/«Плюсы-минусы»/«Ссылки», статус `Предложено`.
- `docs/adr/0003-stek-frontenda.md`, `docs/adr/0007-arhitektura-proekta-moduli.md`, `docs/adr/0014-testovyy-runner-jest-swc.md`.
- `apps/platform/CLAUDE.md`, `apps/platform/src/client/CLAUDE.md` — конвенции кода (React.FC, `Manager`, типы рядом с использованием, циклические `import type`).
- `apps/platform/src/client/project/pages/ProjectPage/ProjectPage.tsx` — текущая точка монтирования (заглушка).

Наши доки:

- `docs/product/features/planner/README.md` — целиком; `01-walls-and-contours.md`, `02-rooms-floors-ceilings.md` — только части про модель данных (что такое контур/точка/сегмент, что выводится); `09-undo-redo.md` — «Snapshot vs command»; `10-save-load.md` — «Версионирование формата»; `08-cameras-views.md` — «Виды», «Смена вида».
- `docs/product/architecture/testing-strategy.md`, `threejs-r185-migration.md` (разделы 1–3), `planner-build-order.md`.
- `docs/product/architecture/competitor-practices-audit.md` (результат задачи 0045) — вердикты keep/rework/drop по практикам конкурента; ADR ссылаются на них, а не переоткрывают.

Референс конкурента (только как источник паттернов и анти-паттернов; код не копируем):

- `00-overview.md` — «Архитектура (сквозная)», «Сводная карта уверенности» (гэпы 2 «жизненный цикл changed-флагов», 3 «маппинг WC-мира в THREE», 6 «bootstrap»).
- `15-ui-redux-bridges.md` — «Главный факт», «Как Redux синхронизируется с движком», «Что не копируем».
- `01-walls.md` и `03-rooms-floors-structure.md` — секции «Data model»; `deep-dives/07-wall-axes-pipeline.md` — вступление про «два мира» (топология vs data/3D) — это готовый аргумент для разделения «хранится/выводится».
- `10-serialization-save-format.md` — полная схема сохранения как ориентир, что вообще должно уметь жить в документе (не копировать форму).
- `09-undo-redo-history.md` — «Snapshot / memento approach» — источник требования B5.

### Метод работы

- Режим оркестратора (пользователь явно просил делать через субагентов): черновики ADR 0015 и 0016 пишут два параллельных субагента по этому брифу; третий — критик на соответствие `docs/adr/README.md` (три секции, один экран, без «Альтернатив»/«Плюсов-минусов»/«Ссылок»), принципам `features/planner/README.md` и решениям аудита; оркестратор сводит и отдаёт автору список развилок. Чтение больших референсных доков — субагентам с конкретными вопросами.
- **Входы, которых не было при написании этого брифа (обязательно):** `docs/product/architecture/competitor-practices-audit.md` — блок «Сквозные архитектурные практики» и таблица «Решения автора (2026-08-17)»; парковка в `planner-build-order.md`. Ключевые решения, влияющие на A/B: конструктор стен — отдельный Canvas2D-вьювер, но **одна модель документа** (все вьюверы — проекции документа); проёмы сохраняются мировыми координатами и пере-дропаются при загрузке, стабильные id осей/граней **не требуются**; `changed`-флаги roomtodo — маркер материала для undo, не гранулярность ребилда (не проектировать «changed-флаги как у конкурента»); recompute синхронный; dirty-флаг автосейва отдельный от истории. Спорные фактические утверждения про конкурента (номера строк, поведение) — не тащить в ADR без проверки; ADR ссылается на наши доки, а не на `plannercore.js`.
- Не выдумывать альтернативы «для галочки»: в «Почему» — только реально отсечённые варианты одной строкой («X отсекается, потому что Y»).
- Библиотеки — готовые (id-генератор, immutable-обновления, шина), не самописные; выбор — с одной строкой обоснования.
- Допустим одноразовый спайк в scratchpad для проверки моста React↔движок или иммутабельного обновления — в репозиторий не коммитить.
- Если решение упирается в вопрос, на который может ответить только автор (продуктовый или архитектурный), — задать его в конце сессии списком, а не блокироваться на первом же.

### Результат

- `docs/adr/0015-arhitektura-dvizhka-planera.md`, `docs/adr/0016-model-dokumenta-planera.md` — статус `Предложено`, по шаблону, каждый в один экран.
- Индекс в `docs/adr/README.md` дополнен двумя строками.
- Раздел «Отложенные технические вопросы» в `planner-build-order.md` пополнен всем, что всплыло.
- Список вопросов автору (если есть) — в «Заметках» этой задачи.

## Приёмка

- [x] Оба ADR отвечают на все пункты «Что решаем» (A1–A10, B1–B7) — либо решением, либо явной пометкой «дорешивается в ADR X» с записью в парковке.
- [x] Ни один пункт из «Что НЕ решаем» не решён внутри ADR; парковка заполнена.
- [x] Формат по `docs/adr/README.md`: три секции, без запрещённых блоков, статус `Предложено`, индекс обновлён.
- [x] Ни одного противоречия с ADR 0003/0007, testing-strategy и принципами `features/planner/README.md`; при найденном противоречии — вопрос автору в «Заметках», а не тихая правка.
- [x] Спека `features/planner/*` и код не изменены.
- [x] После обсуждения с автором и правок — статус `Принято`, эта задача `[x]`.

## Заметки

Сессия 2026-08-17/18: черновики написаны двумя субагентами (0015, 0016), третий — критик; правки сведены оркестратором. Расхождение конфига с ADR 0007 (`@app/core` не запрещён для `planner`) и расхождение ADR 0006 («пакет `uuid`») с фактическим dep `uuidv7` зафиксированы в ADR 0015 «Что важно знать» / стеке, ADR 0006 не правился.

**Вопросы автору (развилки, требующие решения перед `Принято`):**

1. **Пакет сейчас или потом (A3).** ADR 0015: `apps/platform/src/client/planner/` со структурой пакета + ESLint-граница, переезд в `packages/planner-core` позже одним `git mv`. Рекомендация — так и оставить. Альтернатива (пакет сразу) стоит своего `tsconfig`/сборки/правок `moduleNameMapper` при отсутствующей директории `packages/`.
2. **Границы ESLint внутри планера.** ADR 0015 ужесточает конфиг относительно текущего: никаких `@app/*` внутри `planner/` (в т.ч. `@app/core`), `logger` — только DI-пропом. Подтвердить (либо разрешить `@app/core` и поправить ADR 0007).
3. **Где живут камеры видов и активный вид.** Оба ADR согласованы на `Document.view` (одна JSON, одна версия, один round-trip; меняется через неймспейс `view` с событием `view:changed`, в undo не входит — ADR D снапшотит `layout`/`scene`). Альтернатива — состояние движка с отдельной сериализацией рядом с документом (второй сейв/версия). Подтвердить.
4. **Активный вид при загрузке.** Спека 08 «дефолт при старте — конструктор» vs спека 10 «восстанавливается последний ракурс». ADR 0016: восстанавливаются камеры каждого вида, стартовый вид — всегда конструктор. Подтвердить (противоречие спеки — в парковке).
5. **Якорь комнаты `anchor: Id[]` и привязка материалов к концам грани через id точек** — это допустимая «стабильная адресация через id хранимых точек» при решении Q3/Q35 («стабильные id производных не нужны, привязка по площади/совпадению концов»), или автор хочет чисто геометрический якорь (полигон `{x,y}[]` последнего rebuild, как у roomtodo)? ADR 0016 выбрал `Id[]` (спека 10:50 «комната → отсутствующая точка», аудит keep «id-нормализация»).
6. **Проёмы — в общем `scene.items` с `kind: 'door'|'window'|'opening'`**, а не отдельный `openings[]`: единый механизм пере-дропа с настенной мебелью, единый контейнер undo «сцена». Подтвердить.
7. **Cuts (Q12 «areas/cuts сериализуются»)**: ADR 0016 сериализует зоны как контуры и материал cut по ключу, сама грань cut — производная. Подтвердить трактовку.
8. **Углы в документе — градусы** (включая поворот текстур; спека 05 упоминает радианы «внутри»). Подтвердить.
9. **Композиционный корень** — `<Planner />` (ui) создаёт и `PlannerManager`, и `ThreeProjection`. Альтернатива — фабрика `createPlanner()` вне React (удобнее для headless-тестов слоя 2 и Playwright-ручки `getStats()`), но жизненный цикл canvas раздваивается. Подтвердить; как Playwright достаёт `getStats()` (dev-only проп `onReady`, не `window.__*`) — деталь имплементации.
10. **`Result<T,E>` как собственный discriminated union**, а не `neverthrow` — подтвердить (по памяти проекта «не изобретать библиотеки», но тип на две строки).
11. **`FRAME_BUDGET = 5`** — число из аудита (keep «RenderUpdater», desktop); подтвердить или сделать параметром проекции.
12. **Спека 02:138 / 10:19 vs Q23**: нужно ли «восстановление производного без полной пересборки при загрузке», или на загрузке обычный синхронный rebuild (трактовка ADR 0016)?
13. **`immer` auto-freeze включён и в проде** (гарантия «UI не мутирует документ» механическая; цена — заморозка на каждый `produce`, для планов квартир пренебрежима). Подтвердить.

**Решения автора (2026-08-18)** — внесены в ADR 0015/0016 по месту, оба ADR переведены в `Принято`:

1. Пакет сразу: `packages/planner`, npm-имя `@uyutno/planner` (ADR 0002), импорт `from '@uyutno/planner'`; без отдельной сборки — исходники в оба webpack-бандла платформы (SSR — allowlist в `nodeExternals`); настройка tsconfig/webpack/Jest — задача реализации шага 1.
2. ESLint-граница с платформой снята как вопрос: пакет физически не видит `@app/*`, `logger` — только DI; `no-restricted-imports` остаётся для слоёв внутри пакета.
3. Камеры и активный вид — в `Document.view` — подтверждено.
4. Как roomtodo/аудит: явное поле `view.activeView`, при загрузке восстанавливается последний активный вид; противоречие спеки 08 — в парковке («править 08 под 10»).
5. `anchor: Id[]` и привязка материалов к концам грани через id точек — подтверждено.
6. Проёмы в общем `scene.items` с `kind` — подтверждено.
7. Как roomtodo: cuts — явные записи `cuts[]` `{ id, концы — id точек, материал }`, грань/высота — производные.
8. Градусы везде в документе, включая поворот текстур — подтверждено.
9. Композиционный корень — фабрика `createPlanner({ canvas, projectId, logger, … }) → { manager, projection, dispose }` вне React; `<Planner />` — тонкая обёртка (создаёт canvas, зовёт фабрику в эффекте).
10. Свой `Result<T,E>` (discriminated union), не `neverthrow` — подтверждено.
11. `FRAME_BUDGET = 5` — константа-дефолт, переопределяемая параметром проекции — подтверждено.
12. На загрузке — обычный синхронный rebuild — подтверждено; противоречие спек 02:138 / 10:19 — в парковке.
13. immer auto-freeze и в проде — подтверждено.

Следующие задачи: коммит «спека ← ADR» (08 под 10, 02:138/10:19, 05 радианы) и реализация шага 1 (настройка пакета `packages/planner`, правка ADR 0007/0010 по месту).
