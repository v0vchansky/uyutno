# 0083 · TASK · Локальный черновик демо-роута: ключ `planner_demo_draft`, 30 секунд, diff-guard, квота

- Статус: [x]
- Эпик: 0077
- Зависит от: 0081
- Спека: docs/product/features/planner/10-save-load.md («Автосохранение» → «Локальный черновик (localStorage) — только на демо-роуте», «Storage backend и авторизация», «Удаление проектов»); docs/adr/0021-hranenie-dokumenta-planera-i-format-sohraneniya.md («Локальный черновик — только на демо-роуте», «Что важно знать» — клиентский `migrate`)
- Нужен дизайн: нет (механика хранения; модалка «хранилище недоступно» — задача 0065, макет B4)
- Дизайн: —
- PR: —

## Описание

Единственное место в v0, где документ планера живёт в браузере. **В обычном проекте локальной копии нет** (ADR 0021): серверный автосейв её дублирует, а взамен приносит развилку «локальное против серверного» при каждом открытии. На демо-роуте сервера нет вовсе — сохранять некуда, поэтому черновик там нужен.

Задача реализует draft-половину пропа `storage` (`loadDraft`/`saveDraft`/`clearDraft`, заведён в `0081`) и подключает её к таймеру черновика в `persistence`.

Скоуп:

1. **Ключ `planner_demo_draft`** — один на гостевую сессию, **без идентификатора пользователя**. У обычного проекта черновика нет, различать владельцев незачем. Имя ключа — решение ADR 0021, опоры в репозитории у него нет: не искать «как было», брать отсюда.
2. **Периодичность — 30 секунд**, старт с той же задержкой **5 секунд** после старта редактора, что и серверный автосейв (спека 10). Активный drag гасит тик так же, как у серверного автосейва (механика — `0081`).
3. **Diff-guard**: реальный `setItem` идёт только если сериализованный JSON отличается от прошлого — при простое запись пропускается. Опирается на детерминированность `serialize` из `0079` (одинаковый документ даёт байт в байт одинаковую строку), иначе guard не сработает никогда.
4. **Формат записи** — документ в формате сейва (`serialize` из `@uyutno/planner/format`) плюс метка времени (решение автора 2026-08-20, зафиксировано в задаче [0064](0064-TASK-planner-demo-route-and-guest-draft.md)). Версия формата уже внутри конверта, отдельным полем её не дублировать.
5. **Клиентский `migrate` при чтении.** Черновик гостевой сессии сервера не видит, поэтому миграцию формата там выполняет клиент: `loadDraft` прогоняет `migrate` и, если тот что-то изменил, **кладёт результат обратно в тот же ключ**. Код тот же, что на сервере (`@uyutno/planner/format`), точек вызова две.
6. **`QuotaExceededError`** (и любой отказ `localStorage` — приватный режим, отключённое хранилище) — **та же тихая иконка, что и ошибка автосейва**; редактор не падает и работу не теряет. Никаких модалок и тостов. Поведение при квоте — решение ADR 0021, опоры в репозитории у него нет.
7. **Очистка.** `clearDraft` — обязанность платформы, зовётся: при успешном переносе демо-проекта в аккаунт (`0065`) и при удалении проекта из галереи, если демо-ключ ещё жив. Ключ у черновика один и `projectId` в нём нет, поэтому «очистка при удалении проекта» из ADR сводится к безусловному снятию демо-ключа — сопоставлять его с удаляемым проектом нечем и не надо.
8. **Возврат на демо-роут** молча загружает черновик, если он есть, — **без модалки «восстановить?»** (принято по reference, спека 10).
9. Реализация — на стороне платформы (`client/project/lib/`), по конвенциям `src/client/CLAUDE.md`; планер знает только про методы пропа.

Не в задаче: сам демо-роут `/demo`, заготовленный проект и шапка в состоянии «демо» — задача [0064](0064-TASK-planner-demo-route-and-guest-draft.md); гейт Save, модалка «Войдите, чтобы сохранить проект» и её состояние «хранилище недоступно», копия проекта в аккаунт — задача [0065](0065-TASK-planner-demo-save-gate-and-project-copy.md). Здесь — механизм и его тесты; подключение к живому роуту делает `0064`.

## Приёмка

- [x] `saveDraft` пишет в `localStorage` под ключом **`planner_demo_draft`**; идентификатора пользователя в ключе нет.
- [x] Запись идёт раз в **30 с**, первая — не раньше **5 с** после старта редактора.
- [x] **Diff-guard работает**: при неизменном документе `setItem` не вызывается вовсе — тест со счётчиком вызовов на фейковом хранилище.
- [x] `loadDraft` читает черновик, прогоняет `migrate` и при `changed: true` **записывает мигрированный результат обратно** в тот же ключ — отдельный тест с фиктивным шагом миграции.
- [x] Битый/нечитаемый черновик не роняет редактор: `loadDraft` возвращает «нет черновика», ключ не остаётся источником вечной ошибки.
- [x] `QuotaExceededError` при записи не роняет редактор и даёт **то же тихое состояние ошибки**, что и неудачный автосейв; модалки не появляется. _(состояние — да и тестами с обеих сторон; носителя у него на демо нет, см. «Заметки»)_
- [x] Недоступный `localStorage` (обращение бросает синхронно) обрабатывается там же и той же веткой — тест на «хранилище недоступно вообще».
- [x] `clearDraft` снимает ключ; повторный вызов на пустом ключе безопасен.
- [x] Серверного автосейва в режиме черновика нет (гаситель из `0082`), и наоборот: в обычном проекте `saveDraft` не зовётся ни разу — тест на обе стороны.
- [x] `pnpm typecheck`, `pnpm lint`, `pnpm test` зелёные.
- [ ] Prettier чист; ровно один PR. _(`pnpm format` чист; PR не заводился — коммит вне прогона задачи)_

## Заметки

- **Черновик обязан пережить редирект к OAuth-провайдеру и обратно** — иначе попытка сохраниться уничтожает то, ради чего сохранялись. Механика ухода со страницы и возврата описана в задаче `0064` (решение автора 2026-08-20); этой задаче достаточно того, что запись синхронная и лежит в `localStorage`, а не в памяти. Сквозной сценарий проверяет `0064`.
- **План — десятки килобайт JSON при лимите порядка 5 МБ**: запас огромный, ужимать и резать нечего. IndexedDB в v0 не используется.
- **Цикл «демо → логин → работа → логаут»** (спека 10): при логине копия демо-проекта уезжает на сервер отдельным проектом, гостевой ключ очищается, дальнейшая работа пишется только на сервер; после логаута новый демо-сеанс стартует с заготовленного проекта. История не теряется — она лежит в проектах аккаунта.
- **Превью при записи черновика не пересчитывается** (спека 10). В v0 превью нет вовсе — шаг 9.

### Что сдаётся тестами, а не проходом по демо

- **Демо-роута ещё нет** (он появится задачей `0064`), поэтому работа сдаётся механикой и её тестами. Живого прохода «нарисовал → ушёл на вход → вернулся» здесь не было и быть не могло. Не проверено на живом роуте: возврат на демо молча поднимает черновик (пункт 8 скоупа — код есть, вызывающего нет) и переживание редиректа к OAuth-провайдеру (сквозной сценарий — за `0064`, как и записано в «Заметках» выше).
- **Реализация лежит готовой к подключению, но никем не вызывается**: `client/project/lib/demoDraftStorage.ts` отдаёт `demoDraftStorage` — draft-тройку (`loadDraft`/`saveDraft`/`clearDraft`), которую `0064` домешивает к транспорту демо и включает пропом `saveTarget='draft'`. Полного `PlannerStorage` для демо здесь нет намеренно: что делает `save` на демо-роуте, решает гейт логина (`0065`), а не эта задача.
- **`clearDraft` при удалении проекта из галереи не подключён.** Метод готов и покрыт, но вызывать его из `client/projects` значило бы завести встречный runtime-импорт `projects → project` (обратный уже есть: шапка редактора берёт `getProjects` и `RenameProjectModal`) — то есть цикл между модулями. Нужна развилка автора: либо демо-черновик переезжает в общее место (`common`), либо вызов селится там же, где появится демо-роут (`0064`/`0065`), где он и так обязателен при переносе проекта в аккаунт. Практический вес мал: к моменту, когда у пользователя есть галерея, ключ уже снят переносом.

### Открытый вопрос парковки закрыт наполовину — и это осознанно

`QuotaExceededError` ADR 0021 отправляет в «ту же тихую иконку, что и ошибка автосейва», а индикатора на демо нет вовсе (парковка [build-order](../../product/architecture/planner-build-order.md), строка про `0083`). Сделана **механика**, показ оставлен `0064`/`0065`:

- отказ `localStorage` (квота, приватный режим, отключённое хранилище) приезжает в планер объявленной причиной `unknown` и даёт **ровно то же** состояние `persistence`, что неудачный автосейв: `status: 'error'`, `lastError`, `failedAt`, `alert: null`. Редактор не падает, `markSaved` не зовётся, dirty остаётся поднятым — ближайший тик через 30 с пробует снова;
- носителя у этого состояния на демо по-прежнему нет: `saveIndicatorView` (`0084`) в шапке демо не показывается вовсе;
- **найденная деталь для того, кто будет закрывать вопрос**: если носитель когда-нибудь появится, готовый текст не подойдёт — `saveIndicatorView` на `status: 'error'` даёт «Не удалось сохранить **на сервер**», а у черновика сервер ни при чём. То есть решение автора нужно не только про «где показывать», но и про «какими словами».
