/**
 * Слой `document/` — модель документа планера (ADR 0016) и чистая геометрия (`document/geometry/`, ADR C):
 * plain JSON-данные и чистые функции. Ни Three, ни DOM, ни React — энфорсится ESLint (ADR 0015).
 *
 * Заглушка: реализация модели — отдельная задача шага 1 (planner-build-order).
 */
export {};
